[package]
name = "draw-compute"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.31", features = ["wgpu"] }
egui = "0.31"
egui-wgpu = "0.31"
wgpu = { version = "24.0", features = ["spirv"] }

shader = { path = "shader" }
